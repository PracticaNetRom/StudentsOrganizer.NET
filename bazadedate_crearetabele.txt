create table participants (
	idparticipant NUMBER NOT NULL,
	firstname VARCHAR(15),
	lastname VARCHAR(15),
	gender NUMBER,
	birthdate DATE,
	email VARCHAR(10),
	phonenumbers VARCHAR(22),
	faculty VARCHAR(50),
	facultystartyear VARCHAR(30),
	CONSTRAINT participants_id_pk PRIMARY KEY(idparticipant)
);

create table event(
	idevent NUMBER NOT NULL,
	name_event VARCHAR(20),
	period_event VARCHAR(30),
	department VARCHAR(20),
	CONSTRAINT event_id_pk PRIMARY KEY(idevent)
);

create table tasks(
	idtask NUMBER NOT NULL,
	name_task VARCHAR(30),
	CONSTRAINT tasks_id_pk PRIMARY KEY(idtask)
);

create table participantsevent(
	idparticipant NUMBER NOT NULL,
	idevent NUMBER NOT NULL,
	CONSTRAINT participantsevent_id_pk PRIMARY KEY(idparticipant, idevent)	
);

create table parttasks(
	idparticipant NUMBER NOT NULL,
	idtask NUMBER NOT NULL,
	remarks VARCHAR(100),
	CONSTRAINT parttasks_id_pk PRIMARY KEY(idparticipant, idtask)
);

ALTER TABLE participantsevent 
ADD(CONSTRAINT participants_event_fk FOREIGN KEY(idparticipant) 
REFERENCES participants(idparticipant));
ADD(CONSTRAINT participants_event_fk FOREIGN KEY(idevent) 
REFERENCES event(idevent));

ALTER TABLE participants
ADD(CONSTRAINT participants_tasks_fk FOREIGN KEY(idparticipant) 
REFERENCES participants(idparticipant));
ADD(CONSTRAINT participants_tasks_fk FOREIGN KEY(idtask) 
REFERENCES tasks(idtask));

